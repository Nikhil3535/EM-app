stages:
  - docker
  - setup_cluster
  - k8s_deploy

variables:
  DOCKER_IMAGE_BACKEND: "$DOCKER_USERNAME/employee-backend:$CI_COMMIT_SHORT_SHA"
  DOCKER_IMAGE_FRONTEND: "$DOCKER_USERNAME/employee-frontend:$CI_COMMIT_SHORT_SHA"
  AWS_REGION: "us-east-2"
  EKS_CLUSTER_NAME: "EM-cluster"
  DOCKER_TLS_CERTDIR: ""

# -----------------------------
# DOCKER BUILD & PUSH
# -----------------------------
docker_build_push:
  stage: docker
  image: docker:latest
  services:
    - docker:dind

  before_script:
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

  script:
    - docker build -t $DOCKER_IMAGE_BACKEND backend/
    - docker push $DOCKER_IMAGE_BACKEND

    - docker build -t $DOCKER_IMAGE_FRONTEND frontend/
    - docker push $DOCKER_IMAGE_FRONTEND

# -----------------------------
# ONE-TIME CLUSTER SETUP
# -----------------------------
setup_csi_driver:
  stage: setup_cluster
  image: alpine:latest
  when: manual   # 👈 run only once

  before_script:
    - apk add --no-cache curl bash aws-cli

    # install kubectl
    - curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
    - chmod +x kubectl && mv kubectl /usr/local/bin/

    # install helm
    - curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

    # AWS auth
    - export AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID
    - export AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY
    - export AWS_DEFAULT_REGION=$AWS_REGION

    # verify AWS
    - aws sts get-caller-identity

    # connect to EKS
    - aws eks update-kubeconfig --name $EKS_CLUSTER_NAME --region $AWS_REGION

  script:
    - echo "===== INSTALL CSI DRIVER ====="

    - helm repo add secrets-store-csi-driver https://kubernetes-sigs.github.io/secrets-store-csi-driver/charts
    - helm repo update

    - helm upgrade --install csi-secrets-store \
        secrets-store-csi-driver/secrets-store-csi-driver \
        --namespace kube-system \
        --create-namespace \
        --set syncSecret.enabled=true \
        --set installCRDs=true

    - echo "===== INSTALL AWS PROVIDER ====="
    - kubectl apply -f https://raw.githubusercontent.com/aws/secrets-store-csi-driver-provider-aws/main/deployment/aws-provider-installer.yaml

    - echo "===== VERIFY ====="
    - kubectl get crds | grep secretprovider
    - kubectl get pods -n kube-system

# -----------------------------
# KUBERNETES DEPLOY
# -----------------------------
k8s_deploy:
  stage: k8s_deploy
  image: alpine:latest

  before_script:
    - apk add --no-cache curl bash aws-cli python3 py3-pip

    # install kubectl
    - curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
    - chmod +x kubectl && mv kubectl /usr/local/bin/

    # AWS auth
    - export AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID
    - export AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY
    - export AWS_DEFAULT_REGION=$AWS_REGION

    # verify AWS
    - aws sts get-caller-identity

    # connect to EKS
    - aws eks update-kubeconfig --name $EKS_CLUSTER_NAME --region $AWS_REGION

  script:
    - echo "===== APPLY SECRETS FIRST ====="

    # apply secret config first
    - kubectl apply -f secret-provider.yaml

    - echo "===== APPLY SERVICES ====="
    - kubectl apply -f backend-service.yaml
    - kubectl apply -f frontend-service.yaml

    - echo "===== DEPLOY APPLICATION ====="
    - kubectl apply -f backend-deployment.yaml
    - kubectl apply -f frontend-deployment.yaml

    # update images
    - kubectl set image deployment/backend-deployment backend=$DOCKER_IMAGE_BACKEND
    - kubectl set image deployment/frontend-deployment frontend=$DOCKER_IMAGE_FRONTEND

    # restart backend to reload secrets
    - kubectl rollout restart deployment/backend-deployment

    # wait rollout
    - kubectl rollout status deployment/backend-deployment
    - kubectl rollout status deployment/frontend-deployment

    # debug
    - kubectl get pods -o wide
    - kubectl get svc
    - kubectl get secret em-db-secret || true